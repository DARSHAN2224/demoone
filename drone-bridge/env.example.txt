# =============================================================================
# DRONE BRIDGE - PYTHON APPLICATION CONFIGURATION (.env.example)
# =============================================================================

# Backend WebSocket URL (URL for the bridge to send data TO the backend)
BACKEND_WS_URL=ws://localhost:8000/drone

# Bridge's own HTTP Server Port
BRIDGE_HTTP_PORT=8001

# Drone Home Position (e.g., a central point in your city)
DRONE_HOME_LAT=47.6414678
DRONE_HOME_LNG=-122.1401649

# MAVSDK Connection Address (for connecting to PX4 SITL)
MAVSDK_SERVER_ADDRESS=udp://:14540

# Mission Simulation Parameters
DRONE_DEFAULT_ALTITUDE=20.0
DRONE_SIM_SPEED=10.0

# Safety Parameters
BATTERY_MIN_PERCENT_TAKEOFF=30.0
WIND_MAX_M_S=10.0
RAIN_MAX=0.7